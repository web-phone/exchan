use assert_cmd::cmd::Command;

#[test]
fn test_debug_mode() {
    //let result = Command::cargo_bin("exchan")
    //    .unwrap()
    //    .arg("--debug")
    //    .unwrap();

    //assert_eq!(result.status.code(), Some(0));

    // Incompleted test...
    assert!(false);
}
